# Памятка: Настройка сети и траблшутинг

Краткое руководство по базовой настройке сетевых интерфейсов в `etcnet` и решению частых проблем.

## Траблшутинг (решение проблем)

Для диагностики сетевой связности используйте утилиту `ping`.

```shell
# Проверка связи с конкретным IP-адресом
ping 192.168.100.1

# Проверка связи по доменному имени
ping ya.ru
```

Посмотреть текущие настройки интерфейсов и выданные IP-адреса можно с помощью команды `ip a` (или `ip -c a` для цветного вывода).

::: warning Важно
После каждого изменения конфигурационных файлов в `/etc/net/ifaces/` необходимо перезапускать сетевую службу:

```shell
systemctl restart network
```

Если служба не перезапускается или выдаёт ошибку, проверьте её статус:

```shell
systemctl status network
```

или прочитайте логи

```shell
journalctl -xeu network
```

подобным образом можно взаимодействовать и с остальными сервисами, лишь заменяя network на название нужного вам сервиса.
:::

### Типичные проблемы и их решения:

1. **Пингуется IP-адрес в интернете (например, `1.1.1.1` или `8.8.8.8`), но не пингуется доменное имя (`ya.ru`).**
   * **Решение:** Проверьте настройки DNS-сервера. Скорее всего, отсутствует или неверно настроен файл `resolv.conf` для вашего сетевого интерфейса. Убедитесь, что там указан рабочий DNS-сервер, например, `nameserver 8.8.8.8`.

2. **Не пингуется даже внешний IP-адрес (например, `8.8.8.8`).**
   * **Решение:** Проверьте, пингуется ли шлюз по умолчанию, указанный в файле `ipv4route`. Возможно, вы ошиблись в адресе шлюза или забыли его указать.

3. **Пингуется шлюз, но нет доступа в интернет.**
   * **Решение:** Скорее всего, на маршрутизаторе (шлюзе) не настроена трансляция адресов (NAT/Masquerade) или отключён форвардинг пакетов (`net.ipv4.ip_forward = 1`). Подробнее о настройке NAT можно прочитать в [Задании 2](/module1/task1-2.md).

## Создание и настройка интерфейсов

Все сетевые интерфейсы в ALT Linux управляются через подсистему `etcnet`. Конфигурация каждого интерфейса хранится в отдельной директории по пути `/etc/net/ifaces/`.

В рамках стенда у вас будет следующая конфигурация интерфейсов:
* **ISP:** 3 интерфейса (`ens18`, `ens19`, `ens20`)
* **HQ-RTR и BR-RTR:** 2 интерфейса (`ens18`, `ens19`)
* **HQ-SRV, HQ-CLI и BR-SRV:** 1 интерфейс (`ens18`)

Чтобы создать конфигурацию для нового интерфейса, можно скопировать существующую директорию или создать её вручную:

```shell
mkdir /etc/net/ifaces/ens18
```

### Настройка динамического (DHCP) адреса

Таким способом настраивается только `ens18` (интерфейс в сторону интернета) на маршрутизаторе ISP.

```txt:line-numbers {3-7}
# ISP
# /etc/net/ifaces/ens18/options
BOOTPROTO=dhcp
TYPE=eth
CONFIG_IPV4=yes
CONFIG_WIRELESS=no
DISABLED=no
```

Другие файлы (`ipv4address`, `ipv4route`) для DHCP-интерфейса создавать не нужно — все параметры будут получены от сервера провайдера.

### Настройка статического адреса

Этим способом настраиваются все остальные интерфейсы в нашей топологии. При необходимости создайте нужные файлы: `options`, `ipv4address`, `ipv4route`, `resolv.conf`.

**1. Основные параметры (options)**

```txt:line-numbers {3-7}
# /etc/net/ifaces/ens18/options
BOOTPROTO=static
TYPE=eth
CONFIG_IPV4=yes
CONFIG_WIRELESS=no
DISABLED=no
```

**2. IP-адрес и маска (ipv4address)**

В этом файле указывается IP-адрес интерфейса вместе с префиксом маски подсети (в формате CIDR).

```txt:line-numbers {2}
# /etc/net/ifaces/ens18/ipv4address
172.16.1.1/28
```

**3. Шлюз по умолчанию (ipv4route)**

Здесь указывается IP-адрес маршрутизатора, через который будет идти трафик в другие сети.

```txt:line-numbers {2}
# /etc/net/ifaces/ens18/ipv4route
default via 172.16.1.2
```

**4. DNS-сервер (resolv.conf)**

Адрес сервера для разрешения доменных имён. Можно указать публичные серверы (Google, Яндекс) или свой локальный DNS.

```txt:line-numbers {2}
# /etc/net/ifaces/ens18/resolv.conf
nameserver 8.8.8.8
```

::: tip
Если вы хотите указать несколько DNS-серверов, просто добавьте дополнительные строки `nameserver <IP>` каждую с новой строки.
:::
